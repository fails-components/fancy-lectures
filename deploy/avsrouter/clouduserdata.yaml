#cloud-config
write_files:
- content: |
    # configure your avs server instance
    # first nameofyourcluster|secret|URL of the dispatcher
    AVSCONFIG=yourclustername|sharedsecretwithdispatcher|https://urlofyourfailsinstallation/avs
    # the domain name or better hostname of your router
    AVSDOMAIN=hostnameofyourrouter.example.com
    # optionally you can set the timezone, defaults to Europe/Berlin, required to find a restart time in the night
    # AVSTIMEZONE=
    # Set the maximum number of clients per router, default is 50.
    # AVSMAXCLIENTS=50
    # Set the maximum number of lectures per router, default is 10
    # AVSMAXREALMS=10
    # Set the urls for webtransport and websocket, the defaults are constructed from AVSDOMAIN
    # AVSROUTERURL=https://${AVSDOMAIN}/avfails
    # AVSROUTERWSURL=wss://${AVSDOMAIN}/avfails
    #turns of staging, comment if you would like to use cert-bot staging environment
    STAGING=
  path: /home/linux/.env

repo_update: true
repo_upgrade: all

packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common

runcmd:
  - apt-get update -y
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.gpg
  - chmod a+r /etc/apt/keyrings/docker.gpg
  - echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian $(. /etc/os-release && echo $VERSION_CODENAME) stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update -y && apt-get install docker-ce docker-ce-cli docker-compose-plugin -y 
  - wget -O /home/linux/docker-compose.yml https://github.com/fails-components/fancy-lectures/raw/master/deploy/avsrouter/docker-compose.yml
  - cd /home/linux/ && docker compose pull && docker compose up -d
